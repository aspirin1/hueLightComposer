/*global define, console, window, angular*/

define(function () {
    'use strict';

    function ctrl($ionicPlatform, $timeout) {

        //        $ionicPlatform.ready(function () {
        //            $timeout(function () {
        //                if (navigator.splashscreen) {
        //                    navigator.splashscreen.hide();
        //                }
        //            }, 10000);
        //        });
    }

    ctrl.$inject = ['$ionicPlatform', '$timeout'];
    return ctrl;

});
